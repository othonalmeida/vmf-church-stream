# URL da API. Para testar num celular real (Expo Go), use o IP da rede local
# da sua maquina de dev (ex: http://192.168.x.x:4000), nao "localhost" - o
# celular nao consegue resolver "localhost" para a maquina que roda a API.
EXPO_PUBLIC_API_URL=http://192.168.1.100:4000
