# Historico: tentamos "install-strategy=nested" pra corrigir expo/expo-router
# ficando divididos entre a raiz e apps/mobile/node_modules, mas isso criou
# caminhos de arquivo profundos demais (>260 chars) e piorou a performance do
# Metro no Windows. Revertido pro hoisting padrao agora que apps/mobile tem
# todas as dependencias corretas declaradas explicitamente.
